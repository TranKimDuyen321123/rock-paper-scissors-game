# Report (example)

This is a minimal report explaining the project.
- Server pairs two clients and manages a single round.
- Protocol: JSON-over-TCP (newline-delimited)
- To extend: persistent scores, lobby, GUI, multiple rounds.
